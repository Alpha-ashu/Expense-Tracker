{
  "name": "expense-tracker-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Expense Tracker with backend and frontend",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm run test --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "install:all": "npm install && npm run install --prefix backend && npm run install --prefix frontend",
    "clean": "rimraf backend/dist frontend/dist",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5"
  }
}